# MOVE-AI AXIS 환경변수 예시.
# 실제 값은 .env (로컬) 와 Vercel 프로젝트 환경변수(배포)에만 넣는다.
# API key 는 절대 frontend/ 에 두지 않는다. 서버리스 함수(api/)에서만 읽는다.

# --- 생성형 AI (api/ 서버리스 함수 전용) ---
# 미설정 시 /api/health 가 gemini: false 를 반환한다.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
